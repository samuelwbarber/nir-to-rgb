You are gathering experimental results for my MEng final report on a NIR->RGB
translator. You are running on the machine with the `NIR-RGB` repo, its trained
checkpoints under `experiments/`, the full paired dataset under `data/{nir,rgb}`,
a GPU, and (if attached) a Raspberry Pi 5. DO NOT retrain anything and DO NOT
modify model code. Use the existing checkpoints and the repo's own scripts
(`scripts/eval.py`, `scripts/eval_downstream.py`, `scripts/export_onnx.py`,
`scripts/benchmark_onnx.py`, `scripts/benchmark_rknn.py`, `scripts/render_test_predictions.py`).

GROUND RULES
- Never invent or estimate a number. If a run/checkpoint is missing, GPU time
  runs out, or you used a subset, say so explicitly and move on.
- Keep every condition comparable: same test split (seed 42, the 80/10/10 split
  the repo uses), same image size, same sample set across runs. Prefer the FULL
  test set (1,255 images); if you must subsample, use the same indices everywhere
  and record the count.
- The final teacher is `ablation_08_foundation_ensemble_aggressive` (NAFNet
  width 64). The deployed student is the width-24 NAFNet
  (`phase4_student_nafnet24_distill`). Deployment target is the Raspberry Pi 5.
- gap_closure = (translated - nir) / (rgb - nir), per downstream task; 1.0 = matches
  RGB, 0.0 = no better than raw NIR, negative = worse than raw NIR.
- For each eval, point `--config` at that run's own `experiments/<run>/config.yaml`
  and `--checkpoint` at its `checkpoints/best.pth`.

DELIVERABLE
Create a folder `report_results/` containing: (a) all raw JSON/CSV outputs, (b)
all figures as PDF (vector) AND PNG, and (c) a `RESULTS_FOR_REPORT.md` that lists
every headline number with its provenance -- which run, which checkpoint (and
epoch), which split, sample count, image size, date, and the exact command used.
Mark anything incomplete as TODO. This file is the single source of truth I'll
transcribe into the report, so be precise.

TASKS, in priority order:

1. [HIGHEST] Final-teacher downstream gap-closure (the headline result).
   Run the downstream harness on ablation_08 over the full test split, all models:
     python scripts/eval_downstream.py --config experiments/ablation_08_foundation_ensemble_aggressive/config.yaml \
        --checkpoint experiments/ablation_08_foundation_ensemble_aggressive/checkpoints/best.pth \
        --models all --split test --max-samples <full if feasible, else 500>
   Also run the NO-translator baseline (NIR-vs-RGB, omit --checkpoint) on the same
   samples so the gap-closure denominator is exact. Save both JSONs. In the
   markdown, give a per-model table: raw-NIR metric, translated metric, RGB ceiling,
   and gap_closure %, for all 9 models (mp_hands, mp_face, mp_pose, mp_selfie,
   yolo, maskrcnn, deeplab, resnet50, midas).

2. Ablation downstream comparison (the narrative: 01->02->03->04->05->06->08).
   Run the same downstream protocol (same samples) for: ablation_01_baseline,
   02_smartcrop, 03_region_l1, 04_nir_grad, 05_featuremap_256, the best 06
   featuremap_diverse run, and 08. Fill the gaps (03 and 04 had no downstream JSON
   before). Produce ONE comparison table: rows = runs, cols = the headline metric
   per task (or an averaged gap_closure), so the progression is visible. Note for
   each run, in one line, what it changed vs the baseline (from its config).

3. Image-quality table (test split, full): PSNR / SSIM / LPIPS for
   - Pix2Pix U-Net baseline (phase1_unet_pix2pix)
   - NAFNet teacher final (ablation_08)
   - distilled student (phase4_student_nafnet24_distill)
   - the INT8-quantised student (see task 5)
   Use scripts/eval.py --split test for each. One CSV + a markdown table.

4. Qualitative strips (PNG + PDF):
   - Final teacher: a NIR | predicted | GT strip of ~6 test images that INCLUDES
     at least one clear failure case (a person/skin/MediaPipe scene).
   - A baseline-vs-final strip: same scene through ablation_01 vs ablation_08.
   Use scripts/render_test_predictions.py if it does this, else the grid script.
   Record which image stems were used.

5. Edge deployment numbers (Ch5 Edge Deployment section + Results):
   - Confirm the student's exact architecture and parameter count (expected ~6.0M,
     width 24) and the teacher's (expected ~116M).
   - Export the student to ONNX (scripts/export_onnx.py) and produce an INT8
     version via the chosen Pi-5 path. State the runtime you used (ONNX Runtime /
     NCNN / TFLite / Hailo) and the INT8 calibration set size.
   - Measure latency + FPS at input sizes 192, 256, 320 (whichever hit ~10 FPS).
     If the Pi 5 is attached, benchmark on-device; otherwise give ONNX-CPU latency
     as a proxy and clearly label it as not-on-device.
   - Report the INT8-vs-FP quality delta (PSNR/SSIM/LPIPS, and ideally gap_closure
     on a small sample) so I can state the cost of quantisation.

6. Training provenance for the final teacher (ablation_08):
   - From its TB log, report best epoch and the selection metric (val/clip_cos)
     value at that epoch, plus final val PSNR/SSIM/LPIPS and the three feature
     cosine similarities (dinov2/clip/resnet50).
   - Total wall-clock training time and the GPU used.

7. Reproducibility facts (Ch6 Test Plan + Appendix B):
   - Exact dataset counts (confirm 12,536 total; 10,028/1,253/1,255 split) and the
     split seed/regex.
   - The pinned environment: torch/torchvision/CUDA versions, OS, key package
     versions (dump `pip freeze` to report_results/).
   - The public repository URL and the commit hash these results came from.
   - The exact commands to reproduce each table/figure above.

8. Sanity checks (flag, don't hide):
   - MiDaS gap_closure should be high already on raw NIR and must NOT drop after
     translation -- confirm.
   - Note any model where translation makes things WORSE (negative gap_closure).

When done, print the contents of RESULTS_FOR_REPORT.md and a tree of
report_results/.
