\documentclass[9pt]{extarticle}
\usepackage[a4paper,margin=1.1cm]{geometry}
\usepackage{amsmath}
\usepackage{array}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{enumitem}
\setlist{nosep,leftmargin=1.2em}
\renewcommand{\arraystretch}{1.18}
\definecolor{ICLBlue}{RGB}{0,0,205}
\definecolor{Hi}{RGB}{200,30,30}
\pagestyle{empty}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcommand{\hd}[1]{\textbf{\textcolor{ICLBlue}{#1}}}

\begin{document}
\begin{center}
	{\large\bfseries\textcolor{ICLBlue}{NIR-to-RGB Talk — Lectern Cheat-Sheet}}\\[2pt]
	{\footnotesize Budgeted $\approx$\textbf{17:10} (you ran $\approx$12 live). Thread: \emph{judge by recovered behaviour, not image quality.} Set on \#4, pay off \#11 \& \#13.}
\end{center}

\vspace{-2pt}
\renewcommand{\tabcolsep}{4pt}
\begin{tabularx}{\textwidth}{@{}c >{\bfseries}l Y c c@{}}
\toprule
\# & Slide & \textnormal{Say this (one line)} & Min & Cum \\
\midrule
1 & Title & Sam Barber — NIR-to-RGB translator so RGB models work on infrared. & 0:15 & 0:15\\
2 & Why NIR? & Hand: NIR penetrates skin (veins), sees in dark. No colour $\to$ RGB models misread it. & 0:45 & 1:00\\
3 & Problem & Same scene: RGB works, NIR fails. Retraining every model is impractical. & 1:10 & 2:10\\
4 & Idea + thesis & \textcolor{Hi}{Read the box.} Translator in front of a frozen model. Judge by behaviour, not looks. & 1:10 & 3:20\\
5 & Why hard & \textcolor{Hi}{(compress if behind)} One-to-many; parallax; diffusion too slow/stochastic $\to$ 2 bets. & 0:50 & 4:10\\
6 & Data / rig & Beam-splitter $\to$ shared centre $\to$ one homography. 12{,}536 pairs, London. & 1:20 & 5:30\\
7 & Aligning pairs & 5-step pipeline: sharpness, crop, SIFT+RANSAC, warp. Optics make one warp work. & 0:50 & 6:20\\
8 & Core method & \textcolor{Hi}{(give it room)} Swap VGG for DINOv2+CLIP+ResNet. $F(T(x)){\approx}F(\text{RGB})$. & 1:50 & 8:10\\
9 & Deployable & Distil 116M$\to$1.7M (67$\times$), mixed-INT8, 2.3\,MB. LiteRT + Hailo. & 1:00 & 9:10\\
10 & Qualitative & Real outputs. All look plausible — \textcolor{Hi}{watch Pix2Pix (row 2)}. Eye test misleads. & 0:55 & 10:05\\
11 & Why not PSNR & Same person/blue shirt 13\,dB; postbox 18\,dB. \textcolor{Hi}{Wrong one scores higher.} & 0:45 & 10:50\\
12 & Gap closure & 0 = no better than NIR, 1 = full RGB recovered. Model's own RGB as reference. & 1:05 & 11:55\\
13 & Headline & \textcolor{Hi}{SLOW + PAUSE.} Student +ve on all 5. Pix2Pix ties pixels, NEGATIVE downstream. & 1:45 & 13:40\\
14 & Ablation & Drop the ensemble: \textcolor{Hi}{best PSNR but downstream collapses.} Proves it's causal. & 1:00 & 14:40\\
15 & Edge / demo & 5.05\,FPS CPU, 27\,FPS NPU. INT8 faster than FP32. Live app on Pi. & 1:10 & 15:50\\
16 & Limitations & \textcolor{Hi}{(fast)} Behaviour not accuracy; people untested; London-only; sharp frames only. & 0:40 & 16:30\\
17 & Conclusion & \textcolor{Hi}{Name the 3 novelties.} Rig/dataset, NAFNet-first + ensemble, edge student. & 0:40 & 17:10\\
\bottomrule
\end{tabularx}

\vspace{4pt}
\noindent\hd{Pace check:} at the rig (\#6) you should be near \textbf{5:30}. $\approx$17:10 budget (you ran $\approx$12 live). If pressed, drop \#7 (aligning) and compress \#5, \#16.\\
\hd{Always say} ``behaviour agreement'', never ``accuracy''. \quad \hd{Pre-load} the demo asset.

\vspace{6pt}
\noindent{\bfseries\textcolor{ICLBlue}{Q\&A — question $\to$ backup slide}}
\begin{itemize}
	\item \textbf{Circular / pseudo-labels?} $\to$ Independent probes (DINOv2-L, SigLIP2) held out, still close $>$0.5.
	\item \textbf{DINOv2-S / CLIP closures?} $\to$ \emph{Backup: embedding agreement} — higher but training-overlapping, supporting only.
	\item \textbf{Detection weaker than seg?} $\to$ Small objects + fine texture reproduced least faithfully.
	\item \textbf{Why NAFNet?} $\to$ \emph{Backup: why NAFNet} — activation-free $\to$ clean INT8 / edge export.
	\item \textbf{How aligned?} $\to$ \emph{Backup: aligned pairs} — beam-splitter removes parallax $\to$ one homography.
	\item \textbf{How general?} $\to$ \emph{Backup: generalises (and breaks)} — thermal yes, SAR no.
	\item \textbf{Why not diffusion?} $\to$ Iterative + stochastic; violates latency + determinism (F3).
	\item \textbf{Why not fine-tune downstream?} $\to$ Needs weights + labelled NIR per task; out of scope.
\end{itemize}
\noindent{\footnotesize Don't know? Say so, then reason aloud — honesty is marked.}

\end{document}
